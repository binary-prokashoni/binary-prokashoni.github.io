<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8"/>
<meta name="viewport" content="width=device-width,initial-scale=1.0"/>
<title>Projects · Binary Prokashoni</title>
<link rel="preconnect" href="https://fonts.googleapis.com"/>
<link href="https://fonts.googleapis.com/css2?family=DM+Sans:ital,opsz,wght@0,9..40,300;0,9..40,400;0,9..40,500;0,9..40,600;1,9..40,300&family=DM+Mono:wght@400;500&display=swap" rel="stylesheet"/>
<style>
:root{
  --white:#ffffff;
  --off:#fafafa;
  --gray-50:#f5f5f5;
  --gray-100:#ebebeb;
  --gray-200:#d6d6d6;
  --gray-300:#c0c0c0;
  --gray-400:#9a9a9a;
  --gray-600:#5a5a5a;
  --gray-800:#2a2a2a;
  --ink:#111111;
  --radius-sm:6px;
  --radius:12px;
  --radius-lg:20px;
  --shadow-xs:0 1px 3px rgba(0,0,0,.06),0 1px 2px rgba(0,0,0,.04);
  --shadow-sm:0 2px 8px rgba(0,0,0,.07),0 1px 3px rgba(0,0,0,.04);
  --shadow-md:0 8px 24px rgba(0,0,0,.08),0 2px 8px rgba(0,0,0,.04);
  --shadow-lg:0 20px 48px rgba(0,0,0,.10),0 4px 12px rgba(0,0,0,.06);
  --font:'DM Sans',sans-serif;
  --mono:'DM Mono',monospace;
  --ease-out:cubic-bezier(.16,1,.3,1);
}
*,*::before,*::after{box-sizing:border-box;margin:0;padding:0}
html{scroll-behavior:smooth;-webkit-font-smoothing:antialiased;text-rendering:optimizeLegibility}
body{font-family:var(--font);font-size:15px;line-height:1.6;color:var(--ink);background:var(--white);overflow-x:hidden}
img{display:block;max-width:100%}
a{color:inherit;text-decoration:none}
button{font-family:inherit;cursor:pointer;border:none;background:none}
ul{list-style:none}

/* REVEAL */
.reveal{opacity:0;transform:translateY(20px);transition:opacity .65s var(--ease-out),transform .65s var(--ease-out)}
.reveal.visible{opacity:1;transform:none}
.d1{transition-delay:.06s}.d2{transition-delay:.12s}.d3{transition-delay:.18s}
.d4{transition-delay:.24s}.d5{transition-delay:.30s}

/* LAYOUT */
.container{max-width:1080px;margin:0 auto;padding:0 32px}

/* NAV */
#nav{
  position:fixed;top:0;left:0;right:0;z-index:999;
  height:58px;padding:0 32px;
  display:flex;align-items:center;justify-content:space-between;
  background:rgba(255,255,255,.9);
  backdrop-filter:blur(20px) saturate(180%);
  border-bottom:1px solid transparent;
  transition:border-color .2s,box-shadow .2s;
}
#nav.scrolled{border-color:var(--gray-100);box-shadow:var(--shadow-xs)}
.nav-brand{display:flex;align-items:center;gap:10px;font-size:14px;font-weight:600;letter-spacing:-.2px}
.nav-avatar-wrap{width:28px;height:28px;border-radius:7px;background:var(--gray-100);overflow:hidden;flex-shrink:0}
.nav-avatar-wrap img{width:100%;height:100%;object-fit:cover}
.nav-center{display:flex;gap:2px}
.nav-lnk{padding:6px 13px;border-radius:var(--radius-sm);font-size:13.5px;font-weight:500;color:var(--gray-600);transition:color .15s,background .15s}
.nav-lnk:hover,.nav-lnk.active{color:var(--ink);background:var(--gray-50)}
.nav-lnk.active{color:var(--ink);font-weight:600}
.nav-gh{
  display:flex;align-items:center;gap:7px;padding:7px 15px;
  border-radius:var(--radius-sm);background:var(--ink);color:var(--white);
  font-size:13px;font-weight:500;transition:opacity .15s,transform .15s;
}
.nav-gh:hover{opacity:.8;transform:translateY(-1px)}
.nav-gh svg{width:14px;height:14px}
.hamburger{display:none;padding:7px;border-radius:var(--radius-sm)}
.hamburger span{display:block;width:17px;height:1.5px;background:var(--ink);margin:4px 0;transition:.2s}

/* MOBILE MENU */
.mmenu{
  display:none;position:fixed;top:58px;left:0;right:0;z-index:998;
  background:var(--white);border-bottom:1px solid var(--gray-100);
  padding:8px 20px 16px;flex-direction:column;gap:2px;
  box-shadow:var(--shadow-md);
  opacity:0;pointer-events:none;transform:translateY(-6px);
  transition:opacity .2s,transform .2s;
}
.mmenu.open{display:flex;opacity:1;pointer-events:all;transform:none}
.mmenu a{padding:10px 12px;border-radius:var(--radius-sm);font-size:15px;font-weight:500;color:var(--gray-800);transition:background .15s}
.mmenu a:hover{background:var(--gray-50)}

/* ─── PAGE HEADER ─── */
.page-header{
  padding-top:96px;padding-bottom:0;
  border-bottom:1px solid var(--gray-100);
  background:var(--white);
}
.breadcrumb{display:flex;align-items:center;gap:6px;font-family:var(--mono);font-size:11px;color:var(--gray-400);margin-bottom:20px;letter-spacing:.04em}
.breadcrumb a{color:var(--gray-400);transition:color .15s}
.breadcrumb a:hover{color:var(--ink)}
.breadcrumb-sep{color:var(--gray-200)}
.page-header-inner{
  display:flex;align-items:flex-end;justify-content:space-between;gap:24px;
  padding-bottom:28px;flex-wrap:wrap;
}
.ph-left{}
.ph-eyebrow{font-family:var(--mono);font-size:11px;letter-spacing:.07em;color:var(--gray-400);text-transform:uppercase;margin-bottom:8px;display:flex;align-items:center;gap:7px}
.ph-eyebrow .dot{width:5px;height:5px;border-radius:50%;background:var(--gray-300)}
.page-title{font-size:clamp(28px,4vw,42px);font-weight:600;letter-spacing:-1.2px;line-height:1.08;color:var(--ink);margin-bottom:8px}
.page-title em{font-style:italic;font-weight:300;color:var(--gray-600)}
.page-desc{font-size:15px;color:var(--gray-600);max-width:440px;line-height:1.65}
.ph-right{display:flex;align-items:center;gap:8px;flex-shrink:0}
.ph-stat{text-align:right}
.ph-stat-val{font-size:28px;font-weight:600;letter-spacing:-1.2px;color:var(--ink);display:block;font-variant-numeric:tabular-nums;line-height:1}
.ph-stat-key{font-family:var(--mono);font-size:10.5px;color:var(--gray-400);margin-top:3px}
.ph-divider{width:1px;height:36px;background:var(--gray-100);margin:0 4px}

/* ─── STORE TABS ─── */
.store-tabs{display:flex;gap:0;border-bottom:1px solid var(--gray-100);overflow-x:auto;scrollbar-width:none}
.store-tabs::-webkit-scrollbar{display:none}
.tab-btn{
  padding:14px 20px;font-size:13px;font-weight:500;color:var(--gray-400);
  border-bottom:2px solid transparent;white-space:nowrap;transition:color .15s,border-color .15s;
  margin-bottom:-1px;
}
.tab-btn:hover{color:var(--gray-800)}
.tab-btn.active{color:var(--ink);border-bottom-color:var(--ink);font-weight:600}

/* ─── MAIN LAYOUT ─── */
.store-layout{display:grid;grid-template-columns:220px 1fr;gap:0;min-height:calc(100vh - 200px)}

/* ─── SIDEBAR ─── */
.sidebar{
  border-right:1px solid var(--gray-100);
  padding:28px 0;
  position:sticky;top:58px;
  height:calc(100vh - 58px);
  overflow-y:auto;
  scrollbar-width:thin;scrollbar-color:var(--gray-100) transparent;
}
.sidebar-section{margin-bottom:28px;padding:0 20px}
.sidebar-label{font-family:var(--mono);font-size:10px;letter-spacing:.06em;color:var(--gray-300);text-transform:uppercase;margin-bottom:10px;display:block}
.sidebar-search{
  width:100%;height:34px;padding:0 10px 0 30px;
  border:1px solid var(--gray-100);border-radius:var(--radius-sm);
  background:var(--gray-50);font-family:var(--font);font-size:13px;color:var(--ink);
  outline:none;transition:border-color .15s,box-shadow .15s;
}
.sidebar-search:focus{border-color:var(--gray-300);background:var(--white);box-shadow:0 0 0 3px rgba(0,0,0,.04)}
.sidebar-search::placeholder{color:var(--gray-400)}
.sb-srch-wrap{position:relative}
.sb-srch-wrap svg{position:absolute;left:9px;top:50%;transform:translateY(-50%);color:var(--gray-400);width:13px;height:13px;pointer-events:none}
.sidebar-filter-list{display:flex;flex-direction:column;gap:1px}
.sf-item{
  display:flex;align-items:center;justify-content:space-between;
  padding:7px 9px;border-radius:var(--radius-sm);font-size:13px;color:var(--gray-600);
  cursor:pointer;transition:background .12s,color .12s;gap:8px;
}
.sf-item:hover{background:var(--gray-50);color:var(--ink)}
.sf-item.active{background:var(--ink);color:var(--white)}
.sf-item.active .sf-count{background:rgba(255,255,255,.15);color:rgba(255,255,255,.8)}
.sf-left{display:flex;align-items:center;gap:7px;min-width:0}
.sf-dot{width:7px;height:7px;border-radius:50%;flex-shrink:0}
.sf-name{font-size:13px;font-weight:500;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.sf-count{font-family:var(--mono);font-size:10.5px;padding:2px 6px;border-radius:4px;background:var(--gray-100);color:var(--gray-400);flex-shrink:0}
.sort-select{
  width:100%;height:34px;padding:0 10px;
  border:1px solid var(--gray-100);border-radius:var(--radius-sm);
  background:var(--gray-50);font-family:var(--font);font-size:13px;color:var(--ink);
  outline:none;cursor:pointer;transition:border-color .15s;
  -webkit-appearance:none;appearance:none;
  background-image:url("data:image/svg+xml,%3Csvg width='10' height='6' viewBox='0 0 10 6' fill='none' xmlns='http://www.w3.org/2000/svg'%3E%3Cpath d='M1 1l4 4 4-4' stroke='%239a9a9a' stroke-width='1.5' stroke-linecap='round' stroke-linejoin='round'/%3E%3C/svg%3E");
  background-repeat:no-repeat;background-position:right 10px center;padding-right:28px;
}
.sort-select:focus{border-color:var(--gray-300);background-color:var(--white)}

/* ─── MAIN CONTENT ─── */
.store-main{padding:28px 28px;min-width:0}
.store-toolbar{display:flex;align-items:center;justify-content:space-between;margin-bottom:22px;gap:12px;flex-wrap:wrap}
.results-info{font-family:var(--mono);font-size:11.5px;color:var(--gray-400)}
.results-info span{color:var(--ink);font-weight:500}
.view-toggle{display:flex;gap:2px;border:1px solid var(--gray-100);border-radius:var(--radius-sm);padding:3px;background:var(--white)}
.vt-btn{width:28px;height:24px;border-radius:4px;display:flex;align-items:center;justify-content:center;color:var(--gray-400);transition:background .12s,color .12s}
.vt-btn:hover{color:var(--gray-800)}
.vt-btn.active{background:var(--ink);color:var(--white)}
.vt-btn svg{width:13px;height:13px}

/* ─── APP GRID ─── */
.app-grid{
  display:grid;grid-template-columns:repeat(auto-fill,minmax(300px,1fr));
  gap:2px;background:var(--gray-100);border-radius:var(--radius-lg);
  overflow:hidden;min-height:300px;
}
.app-grid.list-view{grid-template-columns:1fr}

/* APP CARD */
.app-card{
  background:var(--white);padding:22px 20px;
  display:flex;flex-direction:column;gap:0;
  cursor:pointer;transition:background .15s;position:relative;
}
.app-card:hover{background:var(--gray-50)}
.app-card-header{display:flex;align-items:flex-start;gap:14px;margin-bottom:12px}
.app-icon{
  width:54px;height:54px;border-radius:14px;
  background:var(--gray-100);flex-shrink:0;overflow:hidden;
  display:flex;align-items:center;justify-content:center;
  font-size:22px;font-weight:600;color:var(--gray-400);
  border:1px solid var(--gray-100);
  position:relative;
}
.app-icon-char{
  font-size:20px;font-weight:700;color:var(--gray-600);
  font-family:var(--mono);text-transform:uppercase;
  line-height:1;
}
.app-icon .lang-badge{
  position:absolute;bottom:-1px;right:-1px;
  width:18px;height:18px;border-radius:5px;
  border:2px solid var(--white);
  display:flex;align-items:center;justify-content:center;
  font-size:7px;font-weight:700;font-family:var(--mono);color:var(--white);
}
.app-meta{flex:1;min-width:0}
.app-name{font-size:14.5px;font-weight:600;letter-spacing:-.2px;color:var(--ink);line-height:1.25;margin-bottom:3px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.app-org{font-size:12px;color:var(--gray-400);margin-bottom:6px;font-family:var(--mono)}
.app-stars{display:flex;align-items:center;gap:3px;font-family:var(--mono);font-size:11px;color:var(--gray-400)}
.app-stars svg{width:10px;height:10px;fill:var(--gray-300)}
.app-get{
  display:flex;align-items:center;justify-content:center;
  padding:5px 14px;border-radius:20px;background:var(--gray-50);border:1px solid var(--gray-200);
  font-size:12.5px;font-weight:600;color:var(--ink);
  transition:all .15s;white-space:nowrap;margin-top:2px;flex-shrink:0;align-self:flex-start;
}
.app-card:hover .app-get{background:var(--ink);color:var(--white);border-color:var(--ink)}

.app-desc{font-size:12.5px;color:var(--gray-600);line-height:1.55;margin-bottom:12px;display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical;overflow:hidden}

/* Screenshots strip */
.app-screenshots{display:flex;gap:6px;margin-bottom:12px;overflow:hidden}
.app-screenshot{
  height:52px;border-radius:7px;background:var(--gray-100);
  flex:1;min-width:0;max-width:90px;
  border:1px solid var(--gray-100);overflow:hidden;
  display:flex;align-items:center;justify-content:center;
  font-family:var(--mono);font-size:8px;color:var(--gray-300);font-weight:500;
}

.app-footer{display:flex;align-items:center;justify-content:space-between;padding-top:11px;border-top:1px solid var(--gray-100);margin-top:auto}
.app-tags{display:flex;gap:4px;flex-wrap:wrap;min-width:0;overflow:hidden}
.app-tag{font-family:var(--mono);font-size:9.5px;padding:2px 6px;border-radius:4px;background:var(--gray-50);color:var(--gray-600);border:1px solid var(--gray-100);white-space:nowrap}
.app-updated{font-family:var(--mono);font-size:10.5px;color:var(--gray-300);flex-shrink:0;margin-left:6px}

/* Visibility badge */
.app-vis{font-family:var(--mono);font-size:9.5px;padding:2px 6px;border-radius:4px;text-transform:uppercase;font-weight:500;flex-shrink:0}
.app-vis.pub{background:#eff6ff;color:#1d6fa0}
.app-vis.priv{background:#fffbeb;color:#92400e}

/* ─── RELEASE BADGE ─── */
.release-badge{
  display:inline-flex;align-items:center;gap:5px;
  font-family:var(--mono);font-size:10px;letter-spacing:.02em;
  padding:3px 8px;border-radius:20px;
  background:#f0fdf4;color:#166534;border:1px solid #dcfce7;
  font-weight:500;flex-shrink:0;
}
.release-badge svg{width:9px;height:9px;fill:#166534}
.no-release{font-family:var(--mono);font-size:10px;color:var(--gray-300)}

/* ─── FEATURED BANNER ─── */
.featured-section{margin-bottom:28px}
.featured-label{font-family:var(--mono);font-size:10.5px;letter-spacing:.06em;color:var(--gray-400);text-transform:uppercase;margin-bottom:14px;display:flex;align-items:center;gap:8px}
.featured-label::after{content:'';flex:1;height:1px;background:var(--gray-100)}
.featured-scroll{display:flex;gap:12px;overflow-x:auto;scrollbar-width:none;padding-bottom:2px}
.featured-scroll::-webkit-scrollbar{display:none}
.featured-card{
  min-width:280px;max-width:280px;border-radius:var(--radius-lg);overflow:hidden;
  background:var(--gray-50);border:1px solid var(--gray-100);
  cursor:pointer;transition:all .2s var(--ease-out);flex-shrink:0;
  display:flex;flex-direction:column;
}
.featured-card:hover{box-shadow:var(--shadow-md);transform:translateY(-2px)}
.featured-banner-img{height:110px;display:flex;align-items:center;justify-content:center;position:relative;overflow:hidden}
.featured-banner-pattern{
  position:absolute;inset:0;
  opacity:.06;
  background-image:radial-gradient(circle at 2px 2px,currentColor 1px,transparent 0);
  background-size:18px 18px;
}
.featured-banner-icon{font-size:32px;position:relative;z-index:1}
.featured-card-body{padding:14px 16px 16px}
.fc-name{font-size:13.5px;font-weight:600;letter-spacing:-.2px;margin-bottom:4px;color:var(--ink)}
.fc-desc{font-size:12px;color:var(--gray-600);line-height:1.5;display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical;overflow:hidden;margin-bottom:10px}
.fc-foot{display:flex;align-items:center;justify-content:space-between}
.fc-release{font-family:var(--mono);font-size:10px;color:var(--gray-400)}
.fc-get{
  padding:4px 12px;border-radius:20px;background:var(--ink);color:var(--white);
  font-size:11.5px;font-weight:600;transition:opacity .15s;
}
.fc-get:hover{opacity:.8}

/* ─── LIST VIEW ─── */
.app-grid.list-view .app-card{
  flex-direction:row;align-items:center;gap:16px;padding:16px 20px;
}
.app-grid.list-view .app-card-header{margin-bottom:0;flex:1;min-width:0}
.app-grid.list-view .app-icon{width:44px;height:44px;border-radius:11px}
.app-grid.list-view .app-desc{display:none}
.app-grid.list-view .app-screenshots{display:none}
.app-grid.list-view .app-footer{border-top:none;padding-top:0;margin-top:0;gap:10px;flex-shrink:0}
.app-grid.list-view .app-tags{display:none}

/* ─── MODAL ─── */
.mbackdrop{
  position:fixed;inset:0;z-index:1000;background:rgba(0,0,0,.35);
  backdrop-filter:blur(6px);display:flex;align-items:center;justify-content:center;padding:20px;
  opacity:0;pointer-events:none;transition:opacity .22s;
}
.mbackdrop.open{opacity:1;pointer-events:all}
.modal{
  background:var(--white);border-radius:var(--radius-lg);max-width:600px;width:100%;
  max-height:88vh;overflow-y:auto;
  transform:translateY(14px) scale(.985);transition:transform .28s var(--ease-out);
  box-shadow:var(--shadow-lg);
}
.mbackdrop.open .modal{transform:none}

/* Modal App Header */
.modal-app-head{
  padding:28px 28px 0;display:flex;gap:18px;align-items:flex-start;
  border-bottom:1px solid var(--gray-100);padding-bottom:22px;
}
.modal-app-icon{
  width:72px;height:72px;border-radius:18px;flex-shrink:0;
  background:var(--gray-100);display:flex;align-items:center;justify-content:center;
  font-size:28px;font-weight:700;color:var(--gray-600);font-family:var(--mono);
  border:1px solid var(--gray-100);text-transform:uppercase;position:relative;overflow:hidden;
}
.modal-app-info{flex:1;min-width:0}
.modal-app-name{font-size:22px;font-weight:600;letter-spacing:-.5px;color:var(--ink);margin-bottom:4px}
.modal-app-org{font-size:13px;color:var(--gray-400);font-family:var(--mono);margin-bottom:10px}
.modal-app-actions{display:flex;align-items:center;gap:8px;flex-wrap:wrap}
.modal-btn-get{
  display:inline-flex;align-items:center;gap:7px;padding:8px 20px;
  border-radius:20px;background:var(--ink);color:var(--white);
  font-size:13px;font-weight:600;transition:opacity .15s;
}
.modal-btn-get:hover{opacity:.8}
.modal-btn-outline{
  display:inline-flex;align-items:center;gap:7px;padding:8px 16px;
  border-radius:20px;border:1px solid var(--gray-200);
  font-size:13px;font-weight:500;color:var(--gray-800);transition:all .15s;
}
.modal-btn-outline:hover{border-color:var(--gray-400)}
.mclose{width:30px;height:30px;border-radius:var(--radius-sm);border:1px solid var(--gray-100);display:flex;align-items:center;justify-content:center;color:var(--gray-600);flex-shrink:0;transition:background .15s,border-color .15s;position:absolute;top:20px;right:20px}
.mclose:hover{background:var(--gray-50);border-color:var(--gray-200)}

.modal-body{padding:22px 28px 28px}
/* Ratings strip */
.modal-stats-row{display:grid;grid-template-columns:repeat(4,1fr);gap:2px;background:var(--gray-100);border-radius:var(--radius);overflow:hidden;margin-bottom:22px}
.modal-stat{background:var(--white);padding:14px 10px;text-align:center}
.modal-stat-val{font-size:21px;font-weight:600;letter-spacing:-1px;color:var(--ink);display:block;font-variant-numeric:tabular-nums}
.modal-stat-key{font-size:10px;color:var(--gray-400);font-family:var(--mono);margin-top:2px}

.modal-section-title{font-size:13px;font-weight:600;color:var(--ink);margin-bottom:10px;letter-spacing:-.1px}
.modal-desc{font-size:14px;color:var(--gray-600);line-height:1.72;margin-bottom:22px}

/* Releases list */
.releases-list{display:flex;flex-direction:column;gap:2px;background:var(--gray-100);border-radius:var(--radius);overflow:hidden;margin-bottom:22px}
.release-item{
  background:var(--white);padding:14px 16px;
  display:flex;align-items:center;gap:12px;
  transition:background .15s;cursor:default;
}
.release-item:hover{background:var(--gray-50)}
.ri-tag{
  font-family:var(--mono);font-size:12px;font-weight:600;color:var(--ink);
  padding:3px 9px;border-radius:6px;background:var(--gray-50);border:1px solid var(--gray-100);flex-shrink:0;
}
.ri-tag.latest{background:#f0fdf4;color:#166534;border-color:#dcfce7}
.ri-name{font-size:13px;font-weight:500;color:var(--ink);flex:1;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.ri-date{font-family:var(--mono);font-size:11px;color:var(--gray-400);flex-shrink:0}
.ri-dl{
  display:flex;align-items:center;gap:5px;font-family:var(--mono);font-size:11px;color:var(--gray-400);
  padding:4px 9px;border-radius:6px;border:1px solid var(--gray-100);background:var(--gray-50);
  transition:all .15s;text-decoration:none;
}
.ri-dl:hover{background:var(--ink);color:var(--white);border-color:var(--ink)}
.ri-dl svg{width:11px;height:11px}
.no-releases-state{padding:28px;text-align:center;color:var(--gray-400);font-family:var(--mono);font-size:12px;background:var(--white)}

.modal-topics{display:flex;flex-wrap:wrap;gap:5px;margin-bottom:20px}
.mtopic{font-family:var(--mono);font-size:10px;padding:3px 7px;border-radius:4px;background:var(--gray-50);color:var(--gray-600);border:1px solid var(--gray-100)}

/* Loading skeleton */
.skeleton{background:linear-gradient(90deg,var(--gray-100) 25%,var(--gray-50) 50%,var(--gray-100) 75%);background-size:200% 100%;animation:shimmer 1.4s infinite;border-radius:6px}
@keyframes shimmer{0%{background-position:200% 0}100%{background-position:-200% 0}}
.skel-card{background:var(--white);padding:22px 20px}
.skel-icon{width:54px;height:54px;border-radius:14px}
.skel-line{height:12px;margin-bottom:8px}
.skel-line.short{width:60%}
.skel-line.long{width:90%}
.skel-line.mid{width:75%}

/* ─── EMPTY / STATE ─── */
.state-box{padding:64px 24px;text-align:center;color:var(--gray-400);font-size:13.5px;display:flex;flex-direction:column;align-items:center;gap:12px;background:var(--white);grid-column:1/-1}
.state-box p{font-family:var(--mono);font-size:12px;color:var(--gray-400)}
.state-box-icon{font-size:32px;margin-bottom:4px}
.spinner{width:22px;height:22px;border:2px solid var(--gray-100);border-top-color:var(--gray-400);border-radius:50%;animation:spin .7s linear infinite}
@keyframes spin{to{transform:rotate(360deg)}}

/* ─── FOOTER ─── */
#footer{border-top:1px solid var(--gray-100);padding:44px 0}
.foot-inner{display:flex;align-items:center;justify-content:space-between;gap:20px;flex-wrap:wrap}
.foot-brand{display:flex;align-items:center;gap:9px;font-size:13.5px;font-weight:600}
.foot-avt{width:26px;height:26px;border-radius:7px;background:var(--gray-100);overflow:hidden}
.foot-avt img{width:100%;height:100%;object-fit:cover}
.foot-links{display:flex;gap:18px}
.foot-lnk{font-size:13px;color:var(--gray-400);transition:color .15s}
.foot-lnk:hover{color:var(--ink)}
.foot-copy{font-family:var(--mono);font-size:11px;color:var(--gray-300)}

/* ─── RESPONSIVE ─── */
@media(max-width:840px){
  .nav-center,.nav-gh{display:none}
  .hamburger{display:block}
  .container{padding:0 20px}
  #nav{padding:0 20px}
  .store-layout{grid-template-columns:1fr}
  .sidebar{display:none}
  .store-main{padding:20px 0}
  .page-header-inner{flex-direction:column;align-items:flex-start;gap:16px}
  .ph-right{flex-direction:row;gap:20px}
  .modal-app-head{flex-direction:column}
  .modal-stats-row{grid-template-columns:repeat(2,1fr)}
  .app-grid{grid-template-columns:1fr}
  .featured-card{min-width:240px}
}
@media(max-width:520px){
  .page-title{font-size:26px}
  .featured-card{min-width:220px}
}
</style>
</head>
<body>

<!-- NAV -->
<nav id="nav">
  <a href="index.html" class="nav-brand">
    <div class="nav-avatar-wrap" id="navAvt"></div>
    <span id="navName">Binary Prokashoni</span>
  </a>
  <ul class="nav-center">
    <li><a href="index.html#hero" class="nav-lnk">Home</a></li>
    <li><a href="index.html#members" class="nav-lnk">Team</a></li>
    <li><a href="projects.html" class="nav-lnk active">Projects</a></li>
  </ul>
  <a href="https://github.com/binary-prokashoni" target="_blank" class="nav-gh" rel="noopener">
    <svg viewBox="0 0 24 24" fill="currentColor"><path d="M12 2C6.477 2 2 6.477 2 12c0 4.418 2.865 8.166 6.839 9.489.5.092.682-.217.682-.482 0-.237-.009-.868-.013-1.703-2.782.604-3.369-1.34-3.369-1.34-.454-1.156-1.11-1.463-1.11-1.463-.908-.62.069-.608.069-.608 1.003.07 1.531 1.03 1.531 1.03.892 1.529 2.341 1.087 2.91.832.092-.647.35-1.088.636-1.338-2.22-.253-4.555-1.11-4.555-4.943 0-1.091.39-1.984 1.029-2.683-.103-.253-.446-1.27.098-2.647 0 0 .84-.269 2.75 1.025A9.578 9.578 0 0 1 12 6.836a9.59 9.59 0 0 1 2.504.337c1.909-1.294 2.747-1.025 2.747-1.025.546 1.377.202 2.394.1 2.647.64.699 1.028 1.592 1.028 2.683 0 3.842-2.339 4.687-4.566 4.935.359.309.678.919.678 1.852 0 1.336-.012 2.415-.012 2.741 0 .267.18.578.688.48C19.138 20.163 22 16.418 22 12c0-5.523-4.477-10-10-10z"/></svg>
    GitHub
  </a>
  <button class="hamburger" id="ham" aria-label="Menu" onclick="toggleMenu()">
    <span></span><span></span><span></span>
  </button>
</nav>

<!-- MOBILE MENU -->
<div class="mmenu" id="mmenu">
  <a href="index.html#hero" onclick="toggleMenu()">Home</a>
  <a href="index.html#members" onclick="toggleMenu()">Team</a>
  <a href="Projects.html" onclick="toggleMenu()">Projects</a>
  <a href="https://github.com/binary-prokashoni" target="_blank" rel="noopener">GitHub ↗</a>
</div>

<!-- PAGE HEADER -->
<div class="page-header">
  <div class="container">
    <div class="breadcrumb" style="padding-top:14px">
      <a href="index.html">binary-prokashoni</a>
      <span class="breadcrumb-sep">/</span>
      <span>projects</span>
    </div>
    <div class="page-header-inner">
      <div class="ph-left">
        <div class="ph-eyebrow"><span class="dot"></span>App Store</div>
        <h1 class="page-title">All <em>Projects</em></h1>
        <p class="page-desc">Browse every repository, explore releases, and discover what Binary Prokashoni is building.</p>
      </div>
      <div class="ph-right">
        <div class="ph-stat">
          <span class="ph-stat-val" id="phRepos">—</span>
          <div class="ph-stat-key">Repositories</div>
        </div>
        <div class="ph-divider"></div>
        <div class="ph-stat">
          <span class="ph-stat-val" id="phReleases">—</span>
          <div class="ph-stat-key">Releases</div>
        </div>
        <div class="ph-divider"></div>
        <div class="ph-stat">
          <span class="ph-stat-val" id="phStars">—</span>
          <div class="ph-stat-key">Stars</div>
        </div>
      </div>
    </div>

    <!-- TABS -->
    <div class="store-tabs" id="storeTabs">
      <button class="tab-btn active" data-tab="all" onclick="setTab('all',this)">All</button>
      <button class="tab-btn" data-tab="released" onclick="setTab('released',this)">With Releases</button>
      <button class="tab-btn" data-tab="popular" onclick="setTab('popular',this)">Most Popular</button>
      <button class="tab-btn" data-tab="recent" onclick="setTab('recent',this)">Recently Updated</button>
    </div>
  </div>
</div>

<!-- STORE LAYOUT -->
<div class="container" style="padding-top:0;padding-bottom:0">
<div class="store-layout">

  <!-- SIDEBAR -->
  <aside class="sidebar">
    <div class="sidebar-section">
      <span class="sidebar-label">Search</span>
      <div class="sb-srch-wrap">
        <svg viewBox="0 0 20 20" fill="none" stroke="currentColor" stroke-width="1.8"><circle cx="9" cy="9" r="6"/><path d="m15 15-3.5-3.5" stroke-linecap="round"/></svg>
        <input type="text" class="sidebar-search" id="srchInput" placeholder="Search projects…"/>
      </div>
    </div>
    <div class="sidebar-section">
      <span class="sidebar-label">Language</span>
      <div class="sidebar-filter-list" id="langList">
        <div class="sf-item active" data-lang="all" onclick="setLang('all',this)">
          <div class="sf-left"><span class="sf-name">All Languages</span></div>
          <span class="sf-count" id="allCount">—</span>
        </div>
      </div>
    </div>
    <div class="sidebar-section">
      <span class="sidebar-label">Sort By</span>
      <select class="sort-select" id="sortSelect" onchange="renderApps()">
        <option value="updated">Recently Updated</option>
        <option value="stars">Most Stars</option>
        <option value="forks">Most Forks</option>
        <option value="name">Name A–Z</option>
        <option value="created">Newest First</option>
      </select>
    </div>
  </aside>

  <!-- MAIN -->
  <main class="store-main">

    <!-- FEATURED (only on 'all' tab) -->
    <div class="featured-section" id="featuredSection">
      <div class="featured-label">Featured</div>
      <div class="featured-scroll" id="featuredScroll">
        <!-- populated by JS -->
      </div>
    </div>

    <!-- TOOLBAR -->
    <div class="store-toolbar">
      <div class="results-info"><span id="resultsCount">—</span> projects</div>
      <div class="view-toggle">
        <button class="vt-btn active" id="vtGrid" onclick="setView('grid')" title="Grid view">
          <svg viewBox="0 0 16 16" fill="currentColor"><rect x="1" y="1" width="6" height="6" rx="1"/><rect x="9" y="1" width="6" height="6" rx="1"/><rect x="1" y="9" width="6" height="6" rx="1"/><rect x="9" y="9" width="6" height="6" rx="1"/></svg>
        </button>
        <button class="vt-btn" id="vtList" onclick="setView('list')" title="List view">
          <svg viewBox="0 0 16 16" fill="currentColor"><rect x="1" y="2" width="14" height="2" rx="1"/><rect x="1" y="7" width="14" height="2" rx="1"/><rect x="1" y="12" width="14" height="2" rx="1"/></svg>
        </button>
      </div>
    </div>

    <div class="app-grid" id="appGrid">
      <!-- Skeleton loaders -->
      <div class="skel-card">
        <div style="display:flex;gap:14px;margin-bottom:14px"><div class="skeleton skel-icon"></div><div style="flex:1"><div class="skeleton skel-line short" style="height:14px;margin-bottom:10px"></div><div class="skeleton skel-line mid"></div></div></div>
        <div class="skeleton skel-line long"></div><div class="skeleton skel-line mid" style="margin-top:6px"></div>
      </div>
      <div class="skel-card">
        <div style="display:flex;gap:14px;margin-bottom:14px"><div class="skeleton skel-icon"></div><div style="flex:1"><div class="skeleton skel-line short" style="height:14px;margin-bottom:10px"></div><div class="skeleton skel-line mid"></div></div></div>
        <div class="skeleton skel-line long"></div><div class="skeleton skel-line mid" style="margin-top:6px"></div>
      </div>
      <div class="skel-card">
        <div style="display:flex;gap:14px;margin-bottom:14px"><div class="skeleton skel-icon"></div><div style="flex:1"><div class="skeleton skel-line short" style="height:14px;margin-bottom:10px"></div><div class="skeleton skel-line mid"></div></div></div>
        <div class="skeleton skel-line long"></div><div class="skeleton skel-line mid" style="margin-top:6px"></div>
      </div>
    </div>

  </main>
</div>
</div>

<!-- FOOTER -->
<footer id="footer">
  <div class="container">
    <div class="foot-inner">
      <div class="foot-brand">
        <div class="foot-avt" id="footAvt"></div>
        <span id="footName">Binary Prokashoni</span>
      </div>
      <div class="foot-links">
        <a href="https://github.com/binary-prokashoni" target="_blank" class="foot-lnk" rel="noopener">GitHub ↗</a>
      </div>
      <div class="foot-copy">Powered by GitHub API · No backend</div>
    </div>
  </div>
</footer>

<!-- DETAIL MODAL -->
<div class="mbackdrop" id="mbackdrop">
  <div class="modal" role="dialog" aria-modal="true" style="position:relative">
    <button class="mclose" id="mclose" aria-label="Close">
      <svg width="13" height="13" viewBox="0 0 14 14" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><path d="M1 1l12 12M13 1 1 13"/></svg>
    </button>

    <div class="modal-app-head">
      <div class="modal-app-icon" id="mIcon"></div>
      <div class="modal-app-info">
        <div class="modal-app-name" id="mTitle">—</div>
        <div class="modal-app-org" id="mOrg">binary-prokashoni</div>
        <div class="modal-app-actions">
          <a id="mLink" href="#" target="_blank" rel="noopener" class="modal-btn-get">
            <svg width="13" height="13" viewBox="0 0 24 24" fill="currentColor"><path d="M12 2C6.477 2 2 6.477 2 12c0 4.418 2.865 8.166 6.839 9.489.5.092.682-.217.682-.482 0-.237-.009-.868-.013-1.703-2.782.604-3.369-1.34-3.369-1.34-.454-1.156-1.11-1.463-1.11-1.463-.908-.62.069-.608.069-.608 1.003.07 1.531 1.03 1.531 1.03.892 1.529 2.341 1.087 2.91.832.092-.647.35-1.088.636-1.338-2.22-.253-4.555-1.11-4.555-4.943 0-1.091.39-1.984 1.029-2.683-.103-.253-.446-1.27.098-2.647 0 0 .84-.269 2.75 1.025A9.578 9.578 0 0 1 12 6.836a9.59 9.59 0 0 1 2.504.337c1.909-1.294 2.747-1.025 2.747-1.025.546 1.377.202 2.394.1 2.647.64.699 1.028 1.592 1.028 2.683 0 3.842-2.339 4.687-4.566 4.935.359.309.678.919.678 1.852 0 1.336-.012 2.415-.012 2.741 0 .267.18.578.688.48C19.138 20.163 22 16.418 22 12c0-5.523-4.477-10-10-10z"/></svg>
            View on GitHub
          </a>
          <a id="mLatestRelease" href="#" target="_blank" rel="noopener" class="modal-btn-outline" style="display:none">
            <svg width="12" height="12" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.8"><path d="M8 1v10M4 7l4 4 4-4"/><path d="M1 13h14" stroke-linecap="round"/></svg>
            Download Latest
          </a>
        </div>
      </div>
    </div>

    <div class="modal-body">
      <div class="modal-stats-row">
        <div class="modal-stat"><span class="modal-stat-val" id="mStars">0</span><div class="modal-stat-key">Stars</div></div>
        <div class="modal-stat"><span class="modal-stat-val" id="mForks">0</span><div class="modal-stat-key">Forks</div></div>
        <div class="modal-stat"><span class="modal-stat-val" id="mWatch">0</span><div class="modal-stat-key">Watchers</div></div>
        <div class="modal-stat"><span class="modal-stat-val" id="mIssues">0</span><div class="modal-stat-key">Issues</div></div>
      </div>

      <div class="modal-section-title">Description</div>
      <p class="modal-desc" id="mDesc">—</p>

      <div class="modal-topics" id="mTopics"></div>

      <div class="modal-section-title">Releases</div>
      <div class="releases-list" id="mReleases">
        <div class="no-releases-state">Loading releases…</div>
      </div>

      <div id="mLangLine" style="font-family:var(--mono);font-size:11.5px;color:var(--gray-400);display:flex;align-items:center;gap:5px;margin-top:4px"></div>
    </div>
  </div>
</div>

<script>
/* ── CONFIG ── */
const ORG='binary-prokashoni', API='https://api.github.com';
let allRepos=[], repoReleases={}, activeLang='all', activeSearch='', activeTab='all', currentView='grid';
const fmt=n=>n>=1000?(n/1000).toFixed(1)+'k':String(n);
const $=id=>document.getElementById(id);
function ago(d){const days=Math.floor((Date.now()-new Date(d))/864e5);if(days<1)return'today';if(days<7)return days+'d ago';if(days<30)return Math.floor(days/7)+'w ago';if(days<365)return Math.floor(days/30)+'mo ago';return Math.floor(days/365)+'y ago'}
function avt(id,url){const w=$(id);if(!w||!url)return;const i=new Image();i.src=url;i.style='width:100%;height:100%;object-fit:cover';w.appendChild(i)}

/* ── LANG COLORS ── */
const LC={JavaScript:'#f1e05a',TypeScript:'#3178c6',Python:'#3572A5',HTML:'#e34c26',CSS:'#563d7c',Java:'#b07219','C++':'#f34b7d',C:'#555',Go:'#00ADD8',Rust:'#dea584',PHP:'#4F5D95',Ruby:'#701516',Swift:'#F05138',Kotlin:'#A97BFF',Dart:'#00B4AB',Shell:'#89e051',Vue:'#41b883','Jupyter Notebook':'#DA5B0B','C#':'#178600',R:'#198CE7'};

/* ── ICON COLORS ── */
const ICON_BG=['#f0f4ff','#fff7ed','#f0fdf4','#fdf4ff','#fffbeb','#f0f9ff','#fef2f2','#f5f5f0'];
const ICON_TEXT=['#3b5bdb','#c2410c','#166534','#7c3aed','#92400e','#0369a1','#b91c1c','#44403c'];
function iconColors(name){const idx=Math.abs(name.split('').reduce((a,c)=>a+c.charCodeAt(0),0))%ICON_BG.length;return{bg:ICON_BG[idx],text:ICON_TEXT[idx]}}

/* ── NAV SCROLL ── */
window.addEventListener('scroll',()=>$('nav').classList.toggle('scrolled',scrollY>8),{passive:true});
let mOpen=false;
function toggleMenu(){mOpen=!mOpen;$('mmenu').classList.toggle('open',mOpen)}

/* ── TAB & VIEW ── */
function setTab(tab, btn){
  activeTab=tab;
  document.querySelectorAll('.tab-btn').forEach(b=>b.classList.remove('active'));
  btn.classList.add('active');
  $('featuredSection').style.display=tab==='all'?'':'none';
  renderApps();
}
function setView(v){
  currentView=v;
  const grid=$('appGrid');
  grid.classList.toggle('list-view',v==='list');
  $('vtGrid').classList.toggle('active',v==='grid');
  $('vtList').classList.toggle('active',v==='list');
}
function setLang(lang, el){
  activeLang=lang;
  document.querySelectorAll('.sf-item').forEach(i=>i.classList.remove('active'));
  el.classList.add('active');
  renderApps();
}

/* ── SEARCH ── */
$('srchInput').addEventListener('input',e=>{activeSearch=e.target.value.toLowerCase().trim();renderApps()});
$('sortSelect').addEventListener('change',renderApps);

/* ── FETCH ORG ── */
async function fetchOrg(){
  try{
    const r=await fetch(`${API}/orgs/${ORG}`);
    if(!r.ok)throw 0;
    const d=await r.json();
    if(d.avatar_url){['navAvt','footAvt'].forEach(id=>avt(id,d.avatar_url))}
    const name=d.name||d.login;
    $('navName').textContent=name;
    $('footName').textContent=name;
  }catch{}
}

/* ── FETCH REPOS ── */
async function fetchRepos(){
  try{
    let pg=1,repos=[];
    while(true){
      const r=await fetch(`${API}/orgs/${ORG}/repos?per_page=100&page=${pg}&sort=updated`);
      const b=await r.json();
      if(!Array.isArray(b)||!b.length)break;
      repos=repos.concat(b);
      if(b.length<100)break;
      pg++;
    }
    allRepos=repos;
    $('phRepos').textContent=repos.length;
    $('phStars').textContent=fmt(repos.reduce((s,r)=>s+r.stargazers_count,0));
    buildLangFilter(repos);
    renderApps();
    // fetch releases after render
    fetchAllReleases(repos);
  }catch{
    $('appGrid').innerHTML='<div class="state-box"><div class="state-box-icon">⚠️</div><p>Failed to load repositories.</p></div>';
  }
}

/* ── FETCH ALL RELEASES (batch) ── */
async function fetchAllReleases(repos){
  let totalReleases=0;
  // fetch in parallel batches of 8
  const batchSize=8;
  for(let i=0;i<repos.length;i+=batchSize){
    const batch=repos.slice(i,i+batchSize);
    await Promise.all(batch.map(async repo=>{
      try{
        const r=await fetch(`${API}/repos/${ORG}/${repo.name}/releases?per_page=20`);
        if(!r.ok){repoReleases[repo.id]=[];return}
        const releases=await r.json();
        repoReleases[repo.id]=Array.isArray(releases)?releases:[];
        totalReleases+=repoReleases[repo.id].length;
      }catch{repoReleases[repo.id]=[]}
    }));
  }
  $('phReleases').textContent=totalReleases;
  renderApps(); // re-render with release badges
  renderFeatured();
}

/* ── BUILD LANG FILTER ── */
function buildLangFilter(repos){
  const langs=[...new Set(repos.map(r=>r.language).filter(Boolean))].sort((a,b)=>repos.filter(r=>r.language===b).length-repos.filter(r=>r.language===a).length);
  $('allCount').textContent=repos.length;
  const list=$('langList');
  // keep the "all" item
  const allItem=list.querySelector('[data-lang="all"]');
  list.innerHTML='';
  list.appendChild(allItem);
  langs.forEach(lang=>{
    const count=repos.filter(r=>r.language===lang).length;
    const item=document.createElement('div');
    item.className='sf-item';item.dataset.lang=lang;
    item.onclick=()=>setLang(lang,item);
    item.innerHTML=`<div class="sf-left"><span class="sf-dot" style="background:${LC[lang]||'#888'}"></span><span class="sf-name">${lang}</span></div><span class="sf-count">${count}</span>`;
    list.appendChild(item);
  });
}

/* ── RENDER FEATURED ── */
function renderFeatured(){
  const top=allRepos.filter(r=>r.stargazers_count>0||repoReleases[r.id]?.length>0)
    .sort((a,b)=>b.stargazers_count-a.stargazers_count).slice(0,8);
  if(!top.length){$('featuredSection').style.display='none';return}
  const scroll=$('featuredScroll');
  const BG_PAIRS=[['#e8f4fd','#1a73c8'],['#fdf0e8','#c4621f'],['#edfdf0','#1a6630'],['#f3edfd','#6b21a8'],['#fdf8e1','#856404'],['#ffe8e8','#b91c1c']];
  scroll.innerHTML=top.map((r,i)=>{
    const latestRelease=repoReleases[r.id]?.[0];
    const [bg,fg]=BG_PAIRS[i%BG_PAIRS.length];
    const ic=iconColors(r.name);
    return `<div class="featured-card" onclick="openModal(${r.id})">
      <div class="featured-banner-img" style="background:${bg};color:${fg}">
        <div class="featured-banner-pattern"></div>
        <div style="position:relative;z-index:1;display:flex;align-items:center;gap:10px">
          <div style="width:44px;height:44px;border-radius:12px;background:${ic.bg};border:1px solid rgba(0,0,0,.06);display:flex;align-items:center;justify-content:center;font-family:var(--mono);font-size:17px;font-weight:700;color:${ic.text};letter-spacing:-1px">${r.name.slice(0,2).toUpperCase()}</div>
          <div>
            <div style="font-size:13px;font-weight:600;color:${fg};letter-spacing:-.2px;line-height:1.2">${r.name}</div>
            ${latestRelease?`<div style="font-family:var(--mono);font-size:10px;color:${fg};opacity:.7;margin-top:2px">${latestRelease.tag_name}</div>`:''}
          </div>
        </div>
      </div>
      <div class="featured-card-body">
        <div class="fc-name">${r.name}</div>
        <div class="fc-desc">${r.description||'<span style="color:var(--gray-300)">No description</span>'}</div>
        <div class="fc-foot">
          <div class="fc-release" style="font-family:var(--mono);font-size:10px;color:var(--gray-400)">${latestRelease?'Latest: '+latestRelease.tag_name:'No releases yet'}</div>
          <span class="fc-get">View</span>
        </div>
      </div>
    </div>`;
  }).join('');
}

/* ── SORT REPOS ── */
function sortedRepos(repos){
  const sort=$('sortSelect').value;
  return [...repos].sort((a,b)=>{
    if(sort==='stars')return b.stargazers_count-a.stargazers_count;
    if(sort==='forks')return b.forks_count-a.forks_count;
    if(sort==='name')return a.name.localeCompare(b.name);
    if(sort==='created')return new Date(b.created_at)-new Date(a.created_at);
    return new Date(b.updated_at)-new Date(a.updated_at);
  });
}

/* ── RENDER APPS ── */
function renderApps(){
  let repos=allRepos;
  if(activeTab==='released')repos=repos.filter(r=>repoReleases[r.id]?.length>0);
  if(activeTab==='popular')repos=[...repos].sort((a,b)=>b.stargazers_count-a.stargazers_count).slice(0,20);
  if(activeTab==='recent')repos=[...repos].sort((a,b)=>new Date(b.updated_at)-new Date(a.updated_at)).slice(0,20);
  if(activeLang!=='all')repos=repos.filter(r=>r.language===activeLang);
  if(activeSearch)repos=repos.filter(r=>r.name.toLowerCase().includes(activeSearch)||(r.description||'').toLowerCase().includes(activeSearch)||(r.topics||[]).some(t=>t.includes(activeSearch)));
  repos=sortedRepos(repos);
  $('resultsCount').textContent=repos.length;
  const grid=$('appGrid');
  if(!repos.length){
    grid.innerHTML='<div class="state-box"><div class="state-box-icon">🔍</div><p>No projects match your filters.</p></div>';
    return;
  }
  grid.innerHTML=repos.map(r=>{
    const ic=iconColors(r.name);
    const initials=r.name.slice(0,2).toUpperCase();
    const releases=repoReleases[r.id]||[];
    const latest=releases[0];
    const langColor=r.language?LC[r.language]||'#888':null;
    return `<div class="app-card" data-id="${r.id}" tabindex="0" role="button" aria-label="${r.name}">
      <div class="app-card-header">
        <div class="app-icon" style="background:${ic.bg};border-color:rgba(0,0,0,.05)">
          <span class="app-icon-char" style="color:${ic.text}">${initials}</span>
          ${langColor?`<span class="lang-badge" style="background:${langColor}"></span>`:''}
        </div>
        <div class="app-meta">
          <div class="app-name">${r.name}</div>
          <div class="app-org">binary-prokashoni</div>
          <div class="app-stars">
            <svg viewBox="0 0 12 12"><path d="M6 1l1.55 3.14L11 4.64l-2.5 2.44.59 3.44L6 8.96l-3.09 1.56L3.5 7.08 1 4.64l3.45-.5L6 1z"/></svg>
            ${r.stargazers_count}
            ${latest?`<span style="margin-left:5px;color:var(--gray-300)">·</span><span style="margin-left:5px;color:#166534;font-size:10px">${latest.tag_name}</span>`:''}
          </div>
        </div>
        <span class="app-get">GET</span>
      </div>
      <div class="app-desc">${r.description||'<em style="color:var(--gray-300)">No description</em>'}</div>
      ${r.topics?.length?`<div class="app-screenshots">${r.topics.slice(0,3).map(t=>`<div class="app-screenshot">${t}</div>`).join('')}</div>`:''}
      <div class="app-footer">
        <div class="app-tags">
          <span class="app-vis ${r.private?'priv':'pub'}">${r.private?'Private':'Public'}</span>
          ${r.language?`<span class="app-tag"><span style="display:inline-block;width:6px;height:6px;border-radius:50%;background:${langColor};margin-right:3px;vertical-align:middle"></span>${r.language}</span>`:''}
          ${releases.length>0?`<span class="app-tag" style="color:#166534;background:#f0fdf4;border-color:#dcfce7">${releases.length} release${releases.length>1?'s':''}</span>`:''}
        </div>
        <span class="app-updated">${ago(r.updated_at)}</span>
      </div>
    </div>`;
  }).join('');
  grid.querySelectorAll('.app-card').forEach(c=>{
    c.addEventListener('click',()=>openModal(+c.dataset.id));
    c.addEventListener('keydown',e=>{if(e.key==='Enter')openModal(+c.dataset.id)});
  });
}

/* ── MODAL ── */
async function openModal(id){
  const r=allRepos.find(x=>x.id===id);if(!r)return;
  const ic=iconColors(r.name);
  $('mIcon').textContent=r.name.slice(0,2).toUpperCase();
  $('mIcon').style.background=ic.bg;$('mIcon').style.color=ic.text;
  $('mTitle').textContent=r.name;
  $('mOrg').textContent='binary-prokashoni / '+r.name;
  $('mDesc').textContent=r.description||'No description provided.';
  $('mStars').textContent=r.stargazers_count;
  $('mForks').textContent=r.forks_count;
  $('mWatch').textContent=r.watchers_count;
  $('mIssues').textContent=r.open_issues_count;
  $('mLink').href=r.html_url;
  $('mTopics').innerHTML=(r.topics||[]).map(t=>`<span class="mtopic">${t}</span>`).join('');
  $('mLangLine').innerHTML=(r.language?`<span style="width:8px;height:8px;border-radius:50%;background:${LC[r.language]||'#888'};display:inline-block;margin-right:4px"></span>${r.language} · `:'')+`Updated ${ago(r.updated_at)}`;

  // Show modal early
  $('mbackdrop').classList.add('open');
  document.body.style.overflow='hidden';

  // Show releases
  const releasesList=$('mReleases');
  const cached=repoReleases[r.id];
  if(cached!==undefined){
    renderReleases(releasesList,cached,r);
  } else {
    releasesList.innerHTML='<div class="no-releases-state">Loading releases…</div>';
    try{
      const res=await fetch(`${API}/repos/${ORG}/${r.name}/releases?per_page=20`);
      const releases=await res.json();
      repoReleases[r.id]=Array.isArray(releases)?releases:[];
      renderReleases(releasesList,repoReleases[r.id],r);
    }catch{releasesList.innerHTML='<div class="no-releases-state">Could not load releases.</div>'}
  }
}

function renderReleases(container, releases, repo){
  const latest=releases[0];
  // Update download button
  if(latest&&latest.html_url){
    $('mLatestRelease').href=latest.html_url;
    $('mLatestRelease').style.display='';
  } else {
    $('mLatestRelease').style.display='none';
  }
  if(!releases.length){
    container.innerHTML='<div class="no-releases-state">No releases yet — check GitHub for source code.</div>';
    return;
  }
  container.innerHTML=releases.map((rel,i)=>`
    <div class="release-item">
      <span class="ri-tag${i===0?' latest':''}">${rel.tag_name}</span>
      <span class="ri-name">${rel.name||rel.tag_name}</span>
      <span class="ri-date">${ago(rel.published_at||rel.created_at)}</span>
      <a href="${rel.html_url}" target="_blank" rel="noopener" class="ri-dl" onclick="event.stopPropagation()">
        <svg viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"><path d="M8 2v8M5 7l3 3 3-3"/><path d="M2 12h12"/></svg>
        Source
      </a>
    </div>`).join('');
}

function closeModal(){$('mbackdrop').classList.remove('open');document.body.style.overflow=''}
$('mclose').addEventListener('click',closeModal);
$('mbackdrop').addEventListener('click',e=>{if(e.target===$('mbackdrop'))closeModal()});
document.addEventListener('keydown',e=>{if(e.key==='Escape')closeModal()});

/* ── BOOT ── */
fetchOrg();
fetchRepos();
</script>
</body>
</html>
